# 写作指南

## 内容组织

每篇文章使用一个独立目录：`articles/<topic>/<slug>/`。正文命名为 `README.md`，便于在 GitHub 打开目录时直接阅读；配图存放于该篇文章的 `assets/`。

例如，UE 主题使用 `unreal-engine`，文章目录使用 `frame-from-game-thread-to-screen`。后续出现新主题时，再创建对应目录。

文章状态使用以下几种即可：

| 状态 | 含义 |
| --- | --- |
| 大纲 / 学习中 | 已确定问题，正在阅读资料和整理思路 |
| 草稿 | 正文已开始形成，仍有待验证内容 |
| 待校对 | 主要内容与实验已完成，正在检查表述、配图和引用 |
| 已发布 | 已发布到知乎等平台，正文中记录发布链接 |

发布后仍可更新文章，并在文末记录影响结论的修订。

## 文章信息

从 [通用模板](templates/article.md) 开始。至少写清作者、状态、开始记录与最近更新日期；发布日期在实际发布后填写。

涉及引擎或工具时，记录具体版本、操作系统、图形 API、渲染路径及影响结论的设置。涉及源码时，尽量记录分支或提交版本，避免把某一版本的实现当作长期不变的规则。

## 从问题到证据

先用一两句话说明要回答的问题，再给出读者需要的背景。阅读源码时，围绕数据的产生、传递、使用与释放建立联系。

- **资料中的结论**：给出来源链接、适用版本，以及必要的上下文。
- **自己的推断**：明确标注推断，并说明依据。
- **实验观察**：记录环境、步骤和观察结果，再解释结果支持什么结论。
- **尚未解决的问题**：保留在文末，不将计划做的实验写成已完成。

代码只摘录解释问题所需的部分。写清类或函数名、仓库内路径和版本；代码截取或简化后，注明省略内容。伪代码应直接标为伪代码。

## 配图与附件

配图文件名建议采用 `01-data-flow.png`、`02-thread-timeline.png` 这样的顺序和含义组合。在正文中使用 Markdown 相对链接，并给出图片说明。

每张图尽量只回答一个问题。图表和截图中出现的版本、采样条件或实验参数，应在图注或正文中解释。

在文章的 `assets/README.md` 中记录配图的来源、作者、许可或使用依据，以及是否做过修改。引用的第三方内容保留其原有条款。

Unreal Insights、RenderDoc 等工具产生的大型原始捕获文件不默认加入 Git；`.gitignore` 已排除常见的 `.utrace`、`.ucache`、`.rdc` 和 `.wpix` 文件。文章中保留必要的截图、采集步骤和参数即可。确需共享原始捕获时，再安排附件存储并记录链接。

## 发布到知乎

1. 将完成的内容整理到文章 `README.md`，同步根目录文章索引的状态。
2. 检查段落、代码块、公式、表格与图片。GitHub 的相对图片路径和 Mermaid 图不会自动成为知乎图片，发布时需上传对应图片并检查预览。
3. 保留参考资料、作者、原文链接和 CC BY 4.0 声明；注明第三方材料的来源与许可。
4. 发布后填写知乎链接、实际发布日期，并将状态改为“已发布”。

## 发布前检查

- [ ] 文章开头的问题在正文中得到回答。
- [ ] 环境、版本和关键配置已经写清。
- [ ] 事实、推断、实验结果与待验证内容有明确区分。
- [ ] 代码片段、图片与引用能够支持相应结论。
- [ ] 本地链接与图片引用有效，模板占位内容已处理。
- [ ] 已更新文章索引、日期、参考资料和许可说明。

本指南是仓库的写作约定，可以随实际写作需要调整。
